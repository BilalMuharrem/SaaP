Flask>=3.1.0
Flask-Login>=0.6.3
Flask-Limiter>=4.1.1
Flask-SQLAlchemy>=3.1.1
# Faz 10A: CSRF koruması — tüm POST form'larında otomatik token doğrulama
Flask-WTF>=1.2.0
SQLAlchemy>=2.0.36
Werkzeug>=3.1.3
gunicorn>=23.0.0
python-dotenv>=1.0.1
celery>=5.3.6
redis>=5.0.1
playwright>=1.42.0
playwright-stealth>=1.0.6,<2.0.0
cloudscraper>=1.2.71
curl_cffi>=0.5.10,<0.6.0
setuptools<81
beautifulsoup4>=4.12.3
lxml>=5.1.0
apscheduler>=3.10.4
groq>=0.4.2
pandas>=2.2.1
openpyxl>=3.1.2
pdfkit>=1.0.0
requests>=2.31.0
psycopg2-binary>=2.9.9
# HOTFIX 1.94: PDF üretimi tarayıcı tarafına alındı (html2pdf.js CDN).
# WeasyPrint sistem bağımlılığı (Pango/Cairo) gerektirdiği için kaldırıldı.
# Sadece markdown kalır — server-side Markdown→HTML için.
markdown>=3.5

# ── Observability (Faz 9A) ──
# Sentry SDK + Flask/Celery entegrasyonları. SENTRY_DSN env yoksa no-op.
sentry-sdk[flask]>=2.0,<3.0

# ── Test bağımlılıkları (Faz 4) ──
pytest>=8.0
pytest-flask>=1.3.0
pytest-cov>=5.0
